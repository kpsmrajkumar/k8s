apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-rs
spec:
  replicas: 2
  selector:
    matchLabels:
      name: nginx-rs
  template:
    metadata:
      labels:
        name: nginx-rs
    spec:
      containers:
      - name: nginx
        image: nginx:latest